import { SubscriptionCompany } from './subscription-company';

describe('SubscriptionCompany', () => {
  it('should create an instance', () => {
    expect(new SubscriptionCompany()).toBeTruthy();
  });
});
